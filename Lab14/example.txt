#include<stdio.h>
int main() {
    FILE *p, *q;
    char arr;
    p=fopen("example.txt", "r");
    q=fopen("test3.txt", "w");
    while((arr=fgetc(p))!=EOF) {
        fputc(arr, q);
    }
    printf("Completed\n");
}