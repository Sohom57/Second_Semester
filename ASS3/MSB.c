#include <stdio.h>
#include<string.h>
#include<stdlib.h>
#include<ctype.h>
#include<math.h>
#include<limits.h>
int main(){
  int n, a, b;
  scanf("%d", &n);
  if(n<=0) printf("MSB of %d is not set", n);
  else printf("MSB of %d is set", n);
}
